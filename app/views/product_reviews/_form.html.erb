<%= form_for(@product_review, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @product_review.errors.any? %>
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@product_review.errors.count, "error") %> prohibited this product_review from being saved:</h4>

      <ul>
      <% @product_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :content, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :rating, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :rating, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :product_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :product_id, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
